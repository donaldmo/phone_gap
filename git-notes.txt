$ git init

$ git ls -a

$ git add .

$ git .commit -am 'initial commit'

$ git remote add origin https://github.com/donaldmo/test_app.git

$ git push -u origin master

username for 'https://github.com': donaldmo@gmail.com
password for: donald120

$ git status

git add .

git commit -am 'commit'

git push